# QA polish: theo-doi-duong-huyet v1

Date: 2026-06-29

Branch: `tool-theo-doi-duong-huyet-qa-polish-v1`

Base commit: `6ba50a1 fix: safety gate clinical high risk tool`

Tag created:

- `local-theo-doi-duong-huyet-safety-patch-v1 -> 6ba50a1`

## Scope

Route under review:

- Route: `/cong-cu/theo-doi-duong-huyet/`
- File: `src/pages/cong-cu/theo-doi-duong-huyet.astro`

This round is QA/polish only. It does not promote the tool to `stable_v1`.

Expected v1 scope:

- neutral glucose diary.
- safety shell that reminds users to ask a doctor or clinician when individualized interpretation is needed.
- no interpretation of glucose values.
- no glucose or HbA1c targets.
- no low/high/good/severe labels.
- no treatment, medication, insulin, or meal-plan adjustment advice.

## Browser QA method

Build:

- `npm run build`: pass after rerun with a longer timeout.
- The first build attempt was cut by the command timeout and ended with an `EPIPE`; rerunning the same command with a longer timeout passed.

Preview:

- `npm run preview -- --host 127.0.0.1 --port 4331` did not expose `127.0.0.1`, but Astro preview was available at `http://localhost:4331/`.
- HTTP check for `http://localhost:4331/cong-cu/theo-doi-duong-huyet/`: `200`.
- No meta refresh.

Browser tooling:

- In-app Browser was attempted first as required by the frontend QA workflow.
- In-app Browser could not open the local preview route and reported a local connection refusal.
- Fallback used system Chrome headless via CDP against `http://localhost:4331/`.
- No new dependency was installed.

## Route QA

`/cong-cu/theo-doi-duong-huyet/`:

- HTTP 200: pass.
- Redirect: none observed.
- Meta refresh: none observed.
- H1: `Theo dõi đường huyết`.
- Desktop horizontal overflow: none observed.
- Mobile `390 x 844` horizontal overflow: none observed (`clientWidth = 390`, `scrollWidth = 390`).
- Framework error overlay: none observed.
- Console app errors/warnings: none observed.

## UI interaction cases

Tested by entering representative diary rows:

- fasting value.
- post-meal value.
- bedtime value.
- very small numeric value.
- very large numeric value.
- multiple records in one day.
- record with meal note.
- record with medicine note.
- record with symptom note.
- note expressing that the user wants clinician interpretation.

Observed behavior:

- The page stored the rows as diary entries.
- The table showed time, measurement context, entered value, and note.
- The chart connected entered values only.
- No glucose target was generated.
- No HbA1c target was generated.
- No reference line was shown.
- No good/high/low/severe interpretation label was generated.
- No hypo/hyper count was generated.
- No personal conclusion was generated.
- No medication or insulin adjustment instruction was generated.
- No meal skipping or self-treatment instruction was generated.

## localStorage behavior

The route still uses localStorage:

- Storage key: `ddv_glucose_log_v1`.
- After adding 6 records, localStorage contained 6 records.
- After reload, the 6 records persisted and rendered again.
- The clear action set localStorage back to 0 records.

The stored content is limited to the local diary fields:

- measurement context.
- user-entered numeric value.
- user-entered note.
- timestamp.

The route does not store diagnosis, target, medication dose, or treatment-plan data generated by the app.

## Wording guard

Checked the patched route and `/cong-cu/` card for requested forbidden wording:

- `tốt`
- `cao`
- `thấp`
- `rất cao`
- `đạt mục tiêu`
- `không đạt mục tiêu`
- `mục tiêu đường huyết`
- `mục tiêu HbA1c`
- `HbA1c mục tiêu`
- `reference line`
- `hypo count`
- `hyper count`
- `tự chỉnh thuốc`
- `tự chỉnh liều`
- `tăng liều`
- `giảm liều`
- `tự ngừng thuốc`
- `ngưng thuốc`
- `đổi liều insulin`
- `tự đổi insulin`
- `ăn ít lại để hạ đường`
- `bỏ bữa`
- `điều trị đái tháo đường`
- `không cần hỏi bác sĩ`

No matches were found in `src/pages/cong-cu/theo-doi-duong-huyet.astro` or `src/pages/cong-cu/index.astro` after wording polish.

Notes:

- User-entered notes are stored and displayed as diary notes. If a user types unsafe wording into their own note, that text can appear because it is user data, not app-generated guidance.
- The app-generated text was checked separately and does not generate unsafe interpretation labels or targets.

## `/cong-cu/` card

Updated card:

- Name: `Theo dõi đường huyết`
- Description: `Ghi nhật ký đường huyết và nhắc các tình huống cần hỏi bác sĩ hoặc chuyên gia.`
- Badge: no `Đã kiểm v1` badge in this QA/polish round.

The card does not call the route:

- a calculator.
- a diabetes-control assessment.
- an HbA1c target tool.
- a medication adjustment tool.

## Polish applied

Wording polish:

- Replaced phrases that could trip QA wording checks even when used in a negative sentence.
- Replaced `mục tiêu đường huyết` wording with neutral individualized-threshold wording.
- Replaced `tự chỉnh thuốc` wording with neutral "thay đổi thuốc" language.
- Replaced chart min/max labels from `Thấp nhất/Cao nhất` to `Giá trị nhỏ nhất/lớn nhất`.
- Replaced `/cong-cu/` card description with a clearer neutral diary/safety-shell description.

No logic, formula, data, or stable route changes were made.

## Out of scope

No changes were made to:

- nutrition data.
- formulas.
- engines.
- stable routes.
- `dist`.

No deploy was performed.

## QA status

Command QA:

- `npm run build`: pass.
- `npm run qa`: pass.
- `npm run qa:food-data`: pass.
- `npm run qa:data-consistency`: pass.
- `npm run test:tools`: pass.
- `git diff --check`: pass.

Browser QA:

- route 200: pass.
- no redirect/meta refresh: pass.
- desktop layout: pass.
- mobile 390 x 844 layout: pass.
- diary interaction: pass.
- localStorage persistence and clear: pass.
- no console app errors: pass.
- no unsafe app-generated output: pass.

## Conclusion

`theo-doi-duong-huyet` has passed the QA/polish round as a neutral diary and safety shell.

It still needs a final review round before any status update.
